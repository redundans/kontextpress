import { createElement } from "@wordpress/element";

/**
 * External dependencies
 */
import { View, TouchableWithoutFeedback } from 'react-native';
/**
 * Internal dependencies
 */

import styles from './block-mobile-floating-toolbar.scss';

var FloatingToolbar = function FloatingToolbar(_ref) {
  var children = _ref.children;
  return createElement(TouchableWithoutFeedback, null, createElement(View, {
    style: styles.floatingToolbar
  }, children));
};

export default FloatingToolbar;
//# sourceMappingURL=block-mobile-floating-toolbar.native.js.map